# ScamShield Pi dependencies — Python 3.13 / ARM64 compatible

# Audio capture (needs: sudo apt install portaudio19-dev python3-dev)
PyAudio==0.2.14

# Speech-to-text (local Whisper)
openai-whisper>=20250625

# Gemini API (google-genai replaces deprecated google-generativeai)
google-genai>=1.0.0

# ElevenLabs TTS
elevenlabs>=2.0.0

# Google Nest / Chromecast
pychromecast>=14.0.0
# Pin: newer zeroconf + pychromecast can raise "Zeroconf instance loop must be running" in background threads (RPi)
zeroconf==0.131.0

# ngrok tunnel (startup.py)
pyngrok>=7.0.0

# Twilio SMS
twilio>=9.10.3

# Grove HAT GPIO (rpi-lgpio is the Python 3.13-compatible drop-in for RPi.GPIO)
rpi-lgpio
# grove.py  # Install separately: pip install grove.py

# Serial (SenseCAP)
pyserial==3.5

# FastAPI server
fastapi>=0.135.0
uvicorn[standard]>=0.42.0
pydantic>=2.12.0

# SQLite async (used by server)
aiosqlite>=0.22.0

# Postgres sync (>=3.3.3 required for cp313 ARM64 wheel)
psycopg[binary]>=3.3.3

# Scheduling (sync worker)
schedule==1.2.2

# Environment variables
python-dotenv>=1.2.0

# HTTP client
httpx>=0.28.0

# Logging / utilities
structlog==24.2.0

# Tests
pytest>=8.0.0
